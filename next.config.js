/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    basePath: '/chronic-uveitis-predictor',
    images: {
      unoptimized: true,
    },
  }
  
  module.exports = nextConfig